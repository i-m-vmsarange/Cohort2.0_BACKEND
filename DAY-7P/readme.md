# Day-7 Learnings

1. Node js requires folder to manage files and dependencies

- So we create project folder with package.json using following command:
- npm init -y

2. Install express (creates api endpoints ) and mongoose (connects node js to mongo db) package as we require them to connect to database

3. create server.js file to start our server and create database connection

4. In server.js file require express and mongoose package

5. 
